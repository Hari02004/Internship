A simple Spring Boot application to manage student details and their file information.
Uses Spring Data JPA and MySQL for data storage.
Implements a One-to-Many relationship between students and files.
Supports adding, updating, deleting, and fetching student records.
